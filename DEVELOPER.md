# developer guide

## Dependencies

- pybind11 (install in a venv)

creating and activating the venv:

```bash
python -m venv .venv
source .venv/bin/activate
```

to deactivate the venv:

```
deactivate
```

to make NVIM LSP/ Linter listen to CMake, run this:

```bash
cmake .
```
